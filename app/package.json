{
  "name": "opencode-free-proxy",
  "version": "1.0.0",
  "description": "Standalone proxy for OpenCode Free API with web dashboard and model mapping",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --use-env-proxy server.js",
    "dev": "node --watch --use-env-proxy server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "lru-cache": "^11.5.2"
  }
}
